llm:
  model: "Qwen/Qwen2.5-14B-Instruct"
  backend: "openai"
  max_tokens: 4096

agent:
  max_steps: 1 # Zero-shot direct prompting, no plan, no steps
  max_searches_per_question: 0
  max_reflection_loops: 0

tools:
  search_provider: "ddg"
  search_max_results: 0
